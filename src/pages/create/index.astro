---
import Layout from "../../layouts/Layout.astro";
import CreateLandingBody from "../../components/CreateLandingBody.astro";
import { getHreflangStaticPaths } from "../../lib/hreflang-helpers";
import type { Locale } from "../../i18n/config";

const locale: Locale = "en";
---

<Layout title="Create a free quiz in a few minutes | WizyQuiz" description="Build your own quiz in a few minutes and share it with your friends. Free, no sign-up needed, with a link of its own." locale={locale} hreflangPaths={getHreflangStaticPaths("create")}>
  <CreateLandingBody locale={locale} />
</Layout>
