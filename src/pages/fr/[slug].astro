---
import Layout from "../../layouts/Layout.astro";
import CategoryPage from "../../components/CategoryPage";
import { localePath } from "../../utils/base";
import { t } from "../../i18n/ui";
import { getSlugPaths, resolveCategoryData } from "../../lib/page-helpers";
import { getHreflangCategoryPaths } from "../../lib/hreflang-helpers";
import type { Locale } from "../../i18n/config";

const locale: Locale = "fr";

export function getStaticPaths() {
  return getSlugPaths("fr");
}

const { categorySlug } = Astro.props;
const { category, catQuizzes, featured, availableGameTypes, quizzesByType } = resolveCategoryData(categorySlug, locale);
const lp = (path: string) => localePath(path, locale);
---

<Layout title={`${category.name} Quiz - Test your knowledge`} description={category.seoIntro || category.description} locale={locale} hreflangPaths={getHreflangCategoryPaths(categorySlug)}>
  <CategoryPage
    client:load
    category={category}
    quizzes={catQuizzes}
    featured={featured}
    gameTypes={availableGameTypes}
    quizzesByType={quizzesByType}
    locale={locale}
  />
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "CollectionPage",
    name: `${category.name} Quiz`,
    description: category.seoIntro || category.description,
    url: `${Astro.site?.toString().replace(/\/$/, '')}/fr/${category.slug}`,
    inLanguage: "fr",
    provider: { "@type": "Organization", name: "Wizy", url: Astro.site?.toString() },
    numberOfItems: catQuizzes.length,
  })} />
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    itemListElement: [
      { "@type": "ListItem", position: 1, name: "Accueil", item: `${Astro.site?.toString().replace(/\/$/, '')}/fr/` },
      { "@type": "ListItem", position: 2, name: category.name },
    ]
  })} />
</Layout>
