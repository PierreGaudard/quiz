---
import Layout from "../../../layouts/Layout.astro";
import CreateLandingBody from "../../../components/CreateLandingBody.astro";
import { getHreflangStaticPaths } from "../../../lib/hreflang-helpers";
import type { Locale } from "../../../i18n/config";

const locale: Locale = "fr";
---

<Layout title="Créer un quiz gratuit en quelques minutes | WizyQuiz" description="Crée ton propre quiz en quelques minutes et partage-le avec tes amis. Gratuit, sans inscription, avec un lien rien qu'à lui." locale={locale} hreflangPaths={getHreflangStaticPaths("create")}>
  <CreateLandingBody locale={locale} />
</Layout>
