---
import Layout from "../../../layouts/Layout.astro";
import CreateLandingBody from "../../../components/CreateLandingBody.astro";
import { getHreflangStaticPaths } from "../../../lib/hreflang-helpers";
import type { Locale } from "../../../i18n/config";

const locale: Locale = "es";
---

<Layout title="Crear un quiz gratis en pocos minutos | WizyQuiz" description="Crea tu propio quiz en pocos minutos y compártelo con tus amigos. Gratis, sin registro, con un enlace propio." locale={locale} hreflangPaths={getHreflangStaticPaths("create")}>
  <CreateLandingBody locale={locale} />
</Layout>
