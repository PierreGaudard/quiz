---
import Layout from "../../layouts/Layout.astro";
import { getHreflangStaticPaths } from "../../lib/hreflang-helpers";
import SearchPage from "../../components/SearchPage";
import { getAllQuizzes } from "../../data/quizzes";
import { getCategories } from "../../data/categories";
import type { Locale } from "../../i18n/config";

const locale: Locale = "es";
const allQuizzes = getAllQuizzes(locale);
const categories = getCategories(locale);
---

<Layout title="Buscar quizzes | Wizy" description="Busca entre cientos de quizzes en Wizy. Encuentra quizzes sobre deportes, cine, anime, videojuegos y mucho mas." locale={locale} hreflangPaths={getHreflangStaticPaths("search")} noindex={true}>
  <SearchPage client:load quizzes={allQuizzes} categories={categories} locale={locale} />
</Layout>
